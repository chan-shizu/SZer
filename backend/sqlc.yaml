version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "db/migrations/*.up.sql"
    queries: "db/queries/"
    gen:
      go:
        out: "db"
        package: "db"
        emit_json_tags: true
