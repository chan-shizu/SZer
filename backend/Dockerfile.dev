FROM golang:1.25.5-alpine

WORKDIR /app

# air (ホットリロード) をインストール
RUN go install github.com/air-verse/air@latest

# 依存関係をインストール
COPY go.mod go.sum ./
RUN go mod download

# ソースコードをコピー
COPY . .

EXPOSE 8080

CMD ["air"]
